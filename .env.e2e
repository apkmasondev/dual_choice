# Test-only configuration used by `npm run build:e2e` (Playwright).
# These are NOT real APK destinations — never deploy a build made with this mode.
VITE_CONTACT_URL=https://example.com/contact
VITE_PORTFOLIO_URL=https://example.com/work
VITE_SITE_ORIGIN=https://example.com
